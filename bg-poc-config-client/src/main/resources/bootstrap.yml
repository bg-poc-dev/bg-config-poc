spring:
  application.name: bg-poc-config-client
  cloud.config.uri: http://localhost:8888
  profiles.active: dev
  cloud:
    config:
      username: user
      password: secret

management.security.enabled: false